const usernameRule =
  "Username can contain letters & digits, but no spaces or special characters.";
const emailRule = "Email must be a valid email address";
const passwordRule =
  "Password must contain at least one letter, one digit, & one special character";

export const SIGNUP_RULES = [usernameRule, emailRule, passwordRule];
